{
  "name": "camry",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "PLAYWRIGHT_BROWSERS_PATH=node_modules/.playwright node server.js",
    "dev": "PLAYWRIGHT_BROWSERS_PATH=node_modules/.playwright node server.js",
    "build": "npm install && PLAYWRIGHT_BROWSERS_PATH=node_modules/.playwright npx playwright install --with-deps chromium"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1",
    "playwright": "^1.57.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.8"
  }
}